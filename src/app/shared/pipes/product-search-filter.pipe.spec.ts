import { ProductSearchFilterPipe } from './product-search-filter.pipe';

describe('ProductSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
